# RepoLens Common Package

This package contains shared utilities, core models, and common data validation interfaces used across RepoLens backend applications and analyzers.

## Structure

* `repolens/common/`: Main package source directory
* `tests/`: Unit tests
